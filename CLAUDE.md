# Claude Code 入口

本仓库的 agent 规则全部在 [`AGENTS.md`](AGENTS.md)，它是 Codex、Claude Code、GLM 共用的唯一正本。**开始工作前先读它**——环境（代码在 WSL Ubuntu 里）、协作规则、机械改动清单（项目地图同步、resident home 同步、新增 Admin 配置项的六个位置）都在那里。

Codex 会自动加载 `AGENTS.md`；Claude Code 只自动加载本文件，所以这里把你指过去。

导航顺序见 [`START_HERE.md`](START_HERE.md) 和 [`DOCS_MAP.md`](DOCS_MAP.md)。

本文件只做指路，不放任何独立知识——任何规则改动都写进 `AGENTS.md`，不要写在这里。
